spring:
  datasource:
    url: jdbc:mysql://localhost:3306/systemMail
    username: DBtester
    password: test123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  thymeleaf:
    cache: false